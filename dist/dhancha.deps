# cyrius dep sidecar - stdlib leaves this fold requires in scope.
# Generated by scripts/sync-deps-sidecar.sh from [deps] stdlib in cyrius.cyml.
# NOT the raw `cyrius distlib` output: that emits `kashi_font_data`, a VENDORED module and
# not a stdlib leaf, which makes `cyrius deps` fail in every consumer. See setu's
# docs/development/issues/2026-08-07-distlib-deps-sidecar-under-reports.md (same defect, other direction).
string
fmt
alloc
io
vec
str
syscalls
assert
bench
args
hashmap
fnptr
tagged
